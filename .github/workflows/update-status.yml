name: Update Pool Status

on:
  repository_dispatch:
    types: [update_pool_status]

concurrency:
  group: update-pool-status
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  update:
    if: >-
      contains(toJson(github.event.client_payload), 'T00:00:') ||
      contains(toJson(github.event.client_payload), 'T06:00:') ||
      contains(toJson(github.event.client_payload), 'T12:00:') ||
      contains(toJson(github.event.client_payload), 'T18:00:')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Apply payload (status + history + daily summary)
        env:
          PAYLOAD: ${{ toJson(github.event.client_payload) }}
        run: python3 scripts/apply_payload.py

      - name: Commit data files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add status.json history.json daily_summary.json
          git diff --staged --quiet || git commit -m "data: update history.json [skip ci]"
          git push
